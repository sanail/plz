[workspace]
members = ["cargo:."]

# Config for 'dist'
[dist]
# The preferred dist version to use in CI (Cargo.toml SemVer syntax)
cargo-dist-version = "0.32.0"
# CI backends to support
ci = "github"
# The installers to generate for each app
installers = ["shell", "powershell", "homebrew"]
# Target platforms to build apps for (Rust target-triple syntax)
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin", "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"]
# Path that installers should place binaries in. Not CARGO_HOME: most people who
# install a prebuilt binary have no Rust toolchain at all. The installers add
# this directory to PATH themselves.
install-path = "~/.local/bin"
# Where to host releases
hosting = "github"
# Whether to install an updater program
install-updater = false
# The tap the Homebrew formula is pushed to. Publishing needs a HOMEBREW_TAP_TOKEN
# secret in this repository: the workflow's own GITHUB_TOKEN cannot write to
# another repository.
tap = "sanail/homebrew-tap"
publish-jobs = ["homebrew"]
